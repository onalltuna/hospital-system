package polyclinic;

	//Initialise(Initialize) interface which all types of clinics implement in a different way
	//Initialise interface creates the doctors for clinics and adds them to doctor lists.
public interface Initialise {
	
	public void intialise();

}
